
# AI手办模型精修器 — 完整开发提示词

## 一、项目概述

开发一个 Blender 5.2 LTS 插件，用于将 AI 生成的 3D 手办模型修复为可直接用于 FDM 3D 打印的生产级模型。插件采用**半自动工作流**：AI 完成 80% 预处理，用户在 6 个关键节点确认或微调。最终输出为单文件 `after.3mf`（内含多个独立部件 + 打印元数据）。

---

## 二、输入规范

### 2.1 3D 模型输入
- 用户在 Blender 内框选或导入 AI 生成的 3D 手办模型（支持 FBX/OBJ/GLB 等常见格式）
- 模型拓扑可能存在的典型问题：非流形边、双面、零面积面、薄壁、穿插、不水密

### 2.2 参考图输入
- **四格上传栏**：前、后、左、右四个视角的参考图
- **前视图为必选**，后/左/右为可选（提供越多，AI 分割精度越高）
- 参考图为 AI 生成该手办时使用的提示图或渲染图

---

## 三、半自动处理工作流（从上到下）

### Step 0: 预处理与部件语义识别
**AI 自动执行：**
1. 非流形修复 / 双面删除 / 零面积面清理 / 去重顶点
2. 壁厚全局检测（标红 < 0.8mm 区域）
3. 本地 ONNX 推理：
   - SAM（Segment Anything）分割参考图
   - 自定义手办分割器（figure_seg.onnx）识别部件类型
4. 通过相机投影将 2D 分割结果映射到 3D 模型
5. 自动标记 5 个部件区域：hair / head / body / fabric / base
6. 使用 Geometry Nodes Bundle 传递部件标签（跨 Modifier 无损流转）

**用户确认点 #1：**
- 3D 视图中以彩色半透明叠加显示 5 个部件区域
- 提供画笔工具，用户可增删/修正 AI 分割边界
- 提供"接受"或"全手动重画"选项

---

### Step 1: Hair 头发精修
**AI 自动执行：**
1. 前视图 SAM 分割提取头发 2D Mask
2. Mask 通过相机投影映射到 3D 顶点组（密度场）
3. 调用 Geometry Nodes XPBD Hair 程序化生成发束曲线：
   - 以密度场控制发束分布
   - 自动生成发根到发梢的粗细渐变
4. 默认参数：中等密度、肩部长度、微卷

**用户确认点 #2：**
- 参数面板：密度滑块 / 长度滑块 / 卷曲噪波强度 / 发梢变细程度
- 3D 视图实时预览发束曲线
- 用户调整满意后，点击"生成 Mesh"
- AI 自动执行：Curves → Mesh（水密化，带厚度，可直接打印）
- Fallback：用户可选择保留原模型头发，仅做清理和加厚

---

### Step 2: Head / Body / Leg 比例校准
**AI 自动执行：**
1. 加载布娃娃模板（标准人体拓扑，基于 MPFB/CharMorph 开源资产）
2. 自动刚性对齐：边界框匹配 + 主成分分析（PCA）定向
3. 在 Sculpt Mode 中应用 Scene Project Brush：
   - 将手办局部区域投影到模板对应位置
   - 保留手办原有艺术风格

**用户确认点 #3：**
- 3D 视图显示模板半透明覆盖层
- 提供 Scene Project Brush，用户可局部"刷"取区域微调对齐
- 提供多体型模板切换（Q版 / 正比 / 写实）
- 可选：一键自动骨骼绑定（标准骨骼命名规范）

---

### Step 3: Body 衣体分离
**AI 自动执行：**
1. ONNX 分割模型细分类别：皮肤 / 内衣 / 外衣物 / 装饰物
2. 通过 Geometry Nodes Bundle 传递分类标签：
   ```
   [AI Tag Bundle]
   ├── region: "sleeve_left"
   ├── type: "outer_fabric"
   ├── thickness: 1.2
   └── priority: 2
   ```
3. 膨胀/收缩法分离衣物网格：
   - 衣物区域向外膨胀 → Boolean Difference → 分离出独立衣物对象
4. 衣物内侧自动生成内表面（Solidify + 反向法线修复）
5. 衣物边缘自动 Mesh Bevel（避免 3D 打印锐边断裂）

**用户确认点 #4：**
- 半透明预览分离后的衣物与身体
- 画笔工具调整分离边界（增删衣物区域）
- 滑块调整不同部位的 Solidify 厚度（外袍 / 蕾丝 / 硬装饰）

---

### Step 4: Fabric 布料修复与加厚
**AI 自动执行：**
1. XPBD Cloth Solver 张力场检测：
   - 定位应力集中区域（几何穿插、面片断裂、悬浮片）
2. 自动松弛修复：Relax 迭代 + 最小角度约束
3. 装饰连续性检查：
   - 检测断裂边、悬浮片、非连通组件
   - 自动桥接或标记待修复
4. 按部位自动应用 Solidify 厚度：
   - 外袍/大衣：1.2mm
   - 普通衣物：1.0mm
   - 蕾丝/薄纱：0.8mm
   - 硬装饰（铠甲/配饰）：1.5mm
5. 3D 打印合规验证：
   - 壁厚检测（全局标红 < 0.8mm）
   - 悬空角检测（> 45° 标黄提示需支撑）
   - 支撑面分析

**用户确认点 #5：**
- 破损区域高亮列表，逐项接受/拒绝 AI 修复
- 壁厚热力图预览
- 滑块微调各部位厚度

---

### Step 5: Base 基座计算
**AI 自动执行：**
1. 计算模型整体重心（含各部位材质密度预设）
2. 凸包分析提取底部支撑面
3. 自动基座生成：
   - 默认圆盘基座，直径 = 模型最宽处 × 1.2
   - 高度 = 3-5mm（FDM 标准）
   - 底部防滑纹理（同心圆或网格）
4. 稳定性模拟：
   - 倾斜角 > 15° 自动扩大基座直径
   - 重心投影必须落在基座范围内
5. 可选：铭牌凹槽（角色名称文本）

**用户确认点 #6：**
- 3 种基座样式预览：圆盘 / 方形 / 角色主题定制
- 滑块调整直径/高度
- 开关：防滑纹理 / 铭牌凹槽
- 稳定性指示器（绿/黄/红）

---

### Step 6: 导出 after.3mf
**AI 自动执行：**
1. 按部件分离为 5 个独立对象：hair / head / body / fabric / base
2. 各部件导出为 3MF Build Items：
   - 每部件包含：Mesh 数据 + 打印方向建议 + 壁厚元数据 + 支撑区域标记
3. 生成打印报告：
   - 总体积与各部件体积
   - 预估耗材重量（按 PLA 密度计算）
   - 壁厚分布图
   - 支撑建议（位置与预估支撑体积）
   - 组装顺序说明
4. 打包为单文件 `after.3mf`（ZIP 压缩，符合 3MF Core Spec + 3MF Production Extension）

**用户确认点：**
- 导出前预览面板：5 个部件列表 + 元数据检查
- 可选择导出部分部件（如只导出 body + fabric）

---

## 四、技术栈

| 层级 | 技术 | 说明 |
|------|------|------|
| 平台 | Blender 5.2 LTS | 2026.07.14 发布，支持至 2028.07 |
| 语言 | Python 3.11 | Blender 内嵌 |
| AI 推理 | ONNX Runtime (CPU/GPU) | 完全本地运行，无需联网 |
| 视觉模型 | SAM (ViT-Tiny) + 自定义 figure_seg.onnx + Depth Anything v2 | 图像分割 + 手办部件识别 + 深度估计 |
| 几何处理 | BMesh + Open3D + Trimesh | 网格修复、布尔、加厚、简化 |
| 程序化生成 | Geometry Nodes (XPBD Hair/Cloth + Bundle) | 发束生成、布料张力检测、标签传递 |
| 雕刻对齐 | Sculpt Mode Scene Project Brush | 局部模板套模 |
| 导出 | 3MF (bpy.ops.export_scene.threemf 封装) | 多部件 + 元数据 |

### 依赖安装
插件首次启动时，自动检测并在 Blender Python 环境中安装：
- `onnxruntime`
- `opencv-python`
- `numpy`
- `Pillow`

模型资产（.onnx 文件）首次使用时自动下载到插件目录，不打包进 addon。

---

## 五、UI 设计规范

### 主面板布局
```
┌─────────────────────────────────────────┐
│  [导入模型]  [上传参考图]  [开始处理]     │
├─────────────────────────────────────────┤
│  四格参考图栏（前/后/左/右）             │
│  [前]*  [后]  [左]  [右]                │
├─────────────────────────────────────────┤
│  进度：Step 0/6 — 部件识别               │
│  [████████░░░░░░░░░░] 33%               │
├─────────────────────────────────────────┤
│  当前步骤：部件语义识别                  │
│  ┌─────────┐  ┌─────────────────────┐  │
│  │ 3D 视图  │  │ 确认面板             │  │
│  │ (彩色   │  │ • 画笔修正工具       │  │
│  │  叠加)  │  │ • 接受 / 跳过 / 重画 │  │
│  │         │  │ • 参数滑块           │  │
│  └─────────┘  └─────────────────────┘  │
├─────────────────────────────────────────┤
│  日志输出                                │
│  [INFO] 检测到 3 处非流形边，已修复      │
└─────────────────────────────────────────┘
```

### 模态面板规范
每个用户确认点使用模态面板：
- **左侧 60%**：3D 视图（带特定预览模式：彩色叠加 / 半透明 / 线框 / 热力图）
- **右侧 40%**：工具与参数（画笔大小、强度、滑块、按钮）
- **底部**：进度条 + 上一步/下一步 + 日志

---

## 六、开源参考与代码借鉴

| 功能 | 参考仓库 | 借鉴点 |
|------|---------|--------|
| 水密修复 | `MeshRepairFor3DPrinting` | 迭代非流形修复算法 |
| 3D 打印工具面板 | `3d-print-toolbox-modified` | UI 架构与清理参数化 |
| 程序化头发 | `BlenderGBHTool` | 曲线发束生成 + 转 Mesh |
| 顶点组分离 | `Vertex_Group_Utils` | 按顶点组拆分对象 |
| 模板骨骼修复 | `VRoidBones` | 模板应用与自动化修复模式 |
| 本地 AI 集成 | `diffused-texture-addon` | Blender 内嵌 Python 依赖管理 + 本地模型加载 |
| 标准人体模板 | `CharMorph` / `MPFB2` | 基础网格拓扑与模板加载机制 |
| 综合资源 | `awesome-blender` | 持续更新的开源插件索引 |

---

## 七、验收标准

### 功能验收
1. `before.fbx` 导入后，插件能在 30 秒内完成 Step 0 部件识别（本地 CPU）
2. 6 个用户确认点均可正常交互，跳过/接受/微调逻辑正确
3. 最终输出 `after.3mf` 包含 5 个独立 Build Items
4. `after.3mf` 可直接在 Cura / PrusaSlicer / Bambu Studio 中打开并正确切片
5. 各部件壁厚 ≥ 0.8mm，无非流形几何

### 质量验收
1. 与参考 `after` 模型对比：
   - 头发拓扑相似度 > 80%（发束走向、覆盖率）
   - 衣体分离准确率 > 90%（边界偏差 < 2mm）
   - 破损修复率 > 90%
   - 基座稳定性：倾斜 15° 不倾倒
2. 打印报告数据误差 < 5%（体积、耗材预估）

---

## 八、交付物

1. **Blender 插件**（.zip 格式，支持 Blender 5.2 LTS）
2. **模型资产包**（.onnx 文件 + 模板 .blend 文件，首次使用自动下载）
3. **用户文档**（安装指南 + 工作流说明 + 故障排查）
4. **GitHub 仓库**（完整源码 + Issues 模板 + 版本发布）
5. **示例输出**：`after.3mf`（基于提供的 before/after 训练对验证）

---

## 九、约束与假设

- 用户已安装 Blender 5.2 LTS（路径 `D:\blender\`）
- 用户电脑支持 CPU 推理（推荐 8GB+ 内存，GPU 可选）
- AI 生成模型的基础拓扑不至于完全破碎（至少主体连通）
- 参考图与 3D 模型为同一角色/姿态（允许角度差异，不允许角色差异）
- FDM 打印参数默认按 PLA 材料、0.4mm 喷嘴、0.2mm 层厚设定

---

## 十、开发环境

- **开发路径**：当前工作区（此处）
- **测试路径**：`D:\blender\`（用户 Blender 5.2 LTS 安装目录）
- **版本控制**：GitHub 仓库
- **迭代方式**：基于提供的 `before.fbx` 与参考 `after` 模型持续对比调优
