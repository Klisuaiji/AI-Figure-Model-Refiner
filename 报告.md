# AI 手办模型精修器 — 开发报告

> 项目：AI Figure Model Refiner（AI 手办模型精修器）
> 目标：将 AI 生成的 3D 手办修复为 FDM 3D 打印可生产模型
> 当前阶段：**Phase 0 — 技术可行性审计（V0.1 准备）**
> 报告日期：2026-08-17
> 审计方式：全部结论基于**实测**（Blender 5.2 无头运行 + 文件系统探测 + 系统探测），禁止假设 API 存在。

---

## 1. 环境（实测）

| 项目 | 值 |
|------|----|
| Blender 版本 | **5.2.0 LTS**（build 2026-07-14，hash `fbe6228777e7`） |
| Blender 路径 | `D:\blender\blender.exe` |
| 内嵌 Python | **3.13.13**（MSC v.1944 64-bit AMD64） |
| Python 可执行 | `D:\blender\5.2\python\bin\python.exe` |
| CPU | AMD Ryzen 7 H 260（8 物理 / 16 逻辑核） |
| 内存 | 15.3 GB |
| GPU | NVIDIA GeForce RTX 5050 Laptop GPU（8GB VRAM）+ AMD Radeon 780M |
| 磁盘 | D: 剩余 51.8 GB，C: 剩余 11.7 GB |
| MCP | `blender-mcp` 插件已安装（v1.6.4），Blender 运行时可注册 |

> 注：Blender 内置插件位于 `D:\blender\5.2\scripts\addons_core\`（如 `io_scene_fbx`）；
> 用户插件目录 `D:\blender\5.2\scripts\addons\` 当前为空，是部署自研插件的落点。

---

## 2. Blender API 调查（实测）

通过 `bpy.ops` 全量遍历 + `bpy.types` 类型探测，关键结论：

- **导入/导出算子**：`import_scene.fbx`、`import_scene.gltf`、`wm.obj_import`、`wm.ply_import`、`wm.stl_import`、`wm.usd_import`、`wm.alembic_import`；导出对应 `export_scene.fbx/gltf`、`wm.obj_export/ply_export/stl_export/usd_export`。
  → **模型导入（FBX/OBJ/GLB/GLTF/STL/PLY）原生支持，无需第三方。**
- **网格算子（极其丰富，全部可用于几何处理）**：
  `mesh.remove_doubles`、`mesh.dissolve_degenerate`、`mesh.fill_holes`、`mesh.normals_make_consistent`、
  `mesh.select_non_manifold`、`mesh.convex_hull`、`mesh.solidify`、`mesh.intersect_boolean`、
  `mesh.separate`、`mesh.decimate`、`mesh.edge_split`、`mesh.bridge_edge_loops`、`mesh.bevel`、
  `object.voxel_remesh`、`object.quadriflow_remesh` 等。
- **底层库可用**：`bmesh`、`mathutils`、`gpu`、`gpu_extras`（全部内嵌）。
- **3MF：不存在原生算子或类型**（`bpy.ops.wm.threemf_export` 不存在；`ThreemfExportSettings` 等类型均为 `False`；磁盘上无任何 3mf 插件）。
  → **3MF 必须自实现**（详见第 4 节）。

---

## 3. Python 依赖调查（实测，Blender 内嵌 Python）

| 包 | 状态 | 版本 | 用途 |
|----|------|------|------|
| `numpy` | ✅ 可用 | 2.3.4 | 数值计算、厚度/矩阵 |
| `mathutils` | ✅ 可用 | — | 向量/矩阵/几何 |
| `bmesh` | ✅ 可用 | — | 拓扑编辑、诊断 |
| `gpu` / `gpu_extras` | ✅ 可用 | — | 叠加层/可视化 |
| `requests` | ✅ 可用 | 2.32.3 | 模型下载（AI 阶段） |
| `scipy` | ❌ 缺失 | — | 高级几何（可选） |
| `PIL` / Pillow | ❌ 缺失 | — | 参考图处理（AI 阶段） |
| `cv2` / OpenCV | ❌ 缺失 | — | 分割/投影（AI 阶段） |
| `trimesh` | ❌ 缺失 | — | 网格分析（可选） |
| `open3d` | ❌ 缺失 | — | 点云/距离（可选） |
| `onnxruntime` | ❌ 缺失 | — | AI 推理（Phase 12） |
| `sklearn` | ❌ 缺失 | — | 模板匹配（可选） |

> **关键决策**：Blender 内嵌 Python 默认**不含** AI/视觉栈（onnx/opencv/PIL）。
> 这印证了规格书 "不要假设这些包可以直接 pip install 到 Blender Python" 的警告。
> V0.1–V0.6 全部可用纯 `bpy + numpy + bmesh` 实现，**不依赖任何缺失包**；
> AI 阶段（Phase 12）将采用**外部 Python worker**（见第 11 节风险 R2）。

---

## 4. 3MF 调查（实测 → 决策）

- 实测：Blender 5.2 **无原生 3MF 导入/导出**。
- 文件系统：磁盘上无任何 3mf 插件或库。
- 第三方选项评估：
  - `lib3mf`（官方 C++ 库，BSD-2-Clause，有 Python 绑定）：可作为**可选加速器**，但非必需。
  - 自实现纯 Python 3MF 导出器：3MF 本质为 **ZIP 包 + XML**（3MF Core Specification + Production Extension），完全可用 `zipfile` + 字符串模板生成，无需任何第三方依赖。
- **决策**：V0.1 起即规划自研 3MF 适配器（ZIP + XML），多 Build Items、单位/坐标/元数据正确；
  Phase 11 实现并实测可被 Bambu Studio / OrcaSlicer / PrusaSlicer / Cura 打开（若无法实测某切片器，将**明确记录**而非声称通过）。
  **绝不调用不存在的 `bpy.ops.export_scene.threemf`。**

---

## 5. Mesh Repair 调查

- 策略：**优先用 BMesh/原生算子自实现**，避免引入 GPL 传染性依赖与版本耦合。
- 参考 `3D Print Toolbox`（`object_print3d_utils`，GPL，Blender 内置）的算法思路（非代码复制），
  自研诊断/修复以完全掌控并便于分发。
- 薄壁/非流形/孔洞/去重/零面积面/法线/连通分量均可由 `bmesh` + 原生算子覆盖。

---

## 6. Hair 技术调查

- 路线：Blender **Curves + Geometry Nodes Hair**（XPBD）程序化生成发束 → Curve→Mesh 水密加厚。
- V0.1 不实现；Phase 5 实现"**优先修复原 AI 头发**"（穿插/悬浮/断裂/过薄/非流形/发梢过薄）→ Solidify/Repair；
  仅当不可修复才进入重建，且必须有 **Fallback（保留原头发 + 清理 + 加厚）**。
- 参考 `BlenderGBHTool`（License 待二次确认，仅参考思路，不复制代码）。

---

## 7. Reference Image 技术调查

- 路线：Blender 图像系统 + Reference Camera + 相机投影 + 3D Ray Casting → 语义概率场。
- 不假设参考图与模型坐标系一致（规格书明确要求对齐模块）。
- V0.1 不实现；Phase 3 实现。

---

## 8. Semantic Segmentation 技术调查

- 第一版（V0.2）：**几何启发 + 用户画笔 + 参考图投影投票**，不依赖自定义 `figure_seg.onnx`。
- AI 版（Phase 12）：SAM / Depth Anything / figure_seg 通过**外部 worker** 推理，结果回传 Blender。
- 多视角投票（Front/Back/Left/Right）生成每个面/顶点的 `semantic_type` + `confidence`。

---

## 9. 开源项目调查（代码复用前均须二次确认 License）

| 项目 | 用途 | License（已知/待确认） | 复用策略 |
|------|------|------------------------|----------|
| Blender（本体） | 平台 | GPL | 仅调用 API |
| 3D Print Toolbox | 清理/诊断思路 | GPL（Blender 内置） | 参考算法，自实现 |
| CharMorph | 人体模板 | GPL-3.0（待确认） | Phase 6 参考，自实现 |
| MPFB2 | 人体模板 | GPL-3.0（待确认） | Phase 6 参考 |
| lib3mf | 3MF 库 | BSD-2-Clause | Phase 11 可选加速（兼容） |
| Blender3mfFormat | 3MF 导出 | 待确认 | 仅参考，自实现为主 |
| BlenderGBHTool | 程序化头发 | 待确认 | Phase 5 参考思路 |
| awesome-blender | 索引 | 待确认 | 检索用 |

> **纪律**：任何第三方代码在复制/改写前必须完成 License 复核，并记录到 `LICENSES.md`（待建）。
> **V0.1 不引入任何外部代码**，核心完全自研，规避传染性许可证风险。

---

## 10. License 调查结论

- 自研核心（BSD-3-Clause 或 GPL 兼容，待定）保持可分发。
- 3MF 若后期接入 `lib3mf`（BSD-2）完全兼容，无传染性。
- AI worker（onnxruntime，MIT）运行在**独立 Python 运行时**，不污染 Blender addon，许可证隔离清晰。

---

## 11. 技术风险矩阵

| ID | 风险 | 等级 | 缓解 |
|----|------|------|------|
| R1 | Blender 无原生 3MF | 中 | 自研纯 Python ZIP+XML 导出器（Phase 11） |
| R2 | Blender Python 缺 onnx/opencv/PIL | 中 | AI 推理改用**外部 Python worker**（本机已具备 3.13.12 运行时），通过 HTTP/子进程通信 |
| R3 | 无训练数据（before/after/参考图） | 低 | 设计本就要求几何启发 + 用户确认；架构预留数据集接口 |
| R4 | AI 分割/重建不可靠 | 低 | 每步 Fallback + 用户画笔接管（规格书核心原则） |
| R5 | 无头环境无法直接驱动 GUI/MCP | 低 | 用 `blender --background --python` 做可复现自动化测试；MCP 供用户交互 |
| R6 | 大型模型性能 | 低 | AI 异步 + 进度/取消/缓存；几何扫描不阻塞正确性 |

---

## 12. 架构建议（模块化，Python 数据为 Source of Truth）

```
AI Figure Model Refiner/
├── addon/ai_figure_refiner/        # 实际 Blender 插件包（部署单元）
│   ├── __init__.py                 # 注册、面板挂载、Preferences
│   ├── core/                       # Session / Pipeline / Logging / Settings / Errors / Utils
│   ├── geometry/                   # diagnostics / repair / thickness / overhang
│   ├── vision/                     # (Phase 3) Reference / Segmentation / Depth / Projection
│   ├── semantic/                   # (Phase 4) Part 语义模型与画笔
│   ├── hair/  fabric/  printing/  export/   # 各阶段模块
│   └── ui/                         # 主面板 / 步骤面板 / 日志
├── scripts/                        # 审计/部署/测试脚本（开发用，不进插件）
├── tests/                          # 单元/集成/几何/回归
├── output/                         # 生成的 3mf / 报告 / 审计 JSON
├── models/  assets/                # (Phase 12) AI 模型 / 模板 .blend
├── 报告.md  README.md  CHANGELOG.md  .gitignore
```

- **RepairSession** 统一记录：session_id / source_model / reference_images / current_step / parts / settings / tasks / validation / export / history。
- **Part** 语义数据：id / name / type / confidence / object / faces / vertices / thickness / status …
- **Task 接口**：`prepare / execute / validate / preview / commit / rollback`，支持 Accept/Reject/Skip/Retry/Undo/Rollback/Manual。
- 开发在**工作区**，测试部署到 `D:\blender\5.2\scripts\addons\ai_figure_refiner\`（提供 `deploy_addon.py`）。

---

## 13. V0.1 开发范围（本次目标）

必须达成（验收 V0.1）：
1. 插件可安装、可在 Blender 偏好设置中启用、无错误注册；
2. N 面板 UI 可启动（导入 / 参考图占位 / 步骤进度 / 日志）；
3. 模型可导入（FBX/OBJ/GLB/GLTF/STL/PLY）；
4. **基础网格诊断可运行**：非流形边、孔洞、重复顶点、零面积面、法线、连通分量、包围盒、体积、三角形数；
5. 日志系统（INFO/WARNING/ERROR/DEBUG）写入 UI + 文件；
6. 打印参数设置（喷嘴/层高/材料/最低壁厚，可配置）；
7. 错误处理 + Undo 快照机制；
8. 无头自动化冒烟测试通过（注册→导入图元→诊断→产出 JSON 报告）。

**不**在 V0.1 实现（架构预留接口）：AI 推理、3MF 导出、参考图系统、语义分割、头发/衣体/基座、打印验证 UI。

---

## 14. 暂不实现功能（明确推迟）

- 自定义 `figure_seg.onnx`、大规模训练集、Ground Truth 自动评价、before/after 差异学习；
- 高精度 AI Hair 重建、完全自动衣物语义识别、完全自动人体模板匹配；
- 3MF 导出（Phase 11）、切片器实测（Phase 11）、AI 自动化（Phase 12）。

---

## 15. 下一步

### 已完成（V0.1，2026-08-17）

1. ✅ **Phase 1**：插件框架（`__init__` 注册、N 面板、RepairSession、Logging、Settings、Errors、Undo 快照、部署脚本）。
2. ✅ **Phase 2（部分）**：网格诊断全部指标 + 基础修复（去重 / 法线 / 补洞）+ Rollback 实测通过。

### 下阶段（V0.2+）

3. **Phase 2 续**：壁厚分析（采样射线法）、Overhang 检测、Floatings、Fragile Features。
4. **Phase 3**：参考图系统（相机映射 + 投影 + Ray Casting）。
5. **Phase 4**：部件语义识别（几何启发 + 用户画笔）。
6. **Phase 11**：3MF 导出器（自研 ZIP + XML）。
7. **Phase 12**：AI 自动化（外部 Python worker）。

每个阶段：无头运行 → 测试 → 修复 → 更新本报告 → Git 提交 → 进入下一阶段。

---

## 16. 第一份报告必备 18 问（快速回答）

1. **当前 Blender 版本？** 5.2.0 LTS（build 2026-07-14）。
2. **Python 版本？** 3.13.13（Blender 内嵌）。
3. **before.fbx 是否存在？** ❌ 工作区中不存在，需用户提供（禁止假装已用）。
4. **after.fbx 是否存在？** ❌ 不存在。
5. **模型三角面数量？** 未提供测试模型，待导入后实测。
6. **before/after 几何差异？** 无数据，无法分析（已记录）。
7. **参考图片是否存在？** ❌ 不存在。
8. **哪些开源项目可复用？** 见第 9 节表；V0.1 不引入外部代码。
9. **每个项目 License？** 见第 9 节（多为 GPL/BSD，复用前二次确认）。
10. **哪些代码可直接用？** 当前阶段不建议直接复制；自研核心。
11. **哪些功能必须自实现？** 3MF 导出、网格诊断/修复、语义/参考图逻辑。
12. **Blender 5.2 是否支持目标 3MF 工作流？** ❌ 不支持原生 3MF → 自实现。
13. **ONNX Runtime 是否可用？** ❌ Blender Python 缺失 → 外部 worker 方案。
14. **SAM 是否可用？** ❌ 当前不可用 → Phase 12 经外部 worker。
15. **当前最大技术风险？** R1（无 3MF）+ R2（无 AI 栈），均有明确 fallback。
16. **V0.1 应实现什么？** 见第 13 节。
17. **哪些推迟到 V0.2/V0.3？** 见第 14 节。
18. **预计模块架构？** 见第 12 节。

---

## 17. 已知限制 / 测试记录

- 无训练数据：所有 before/after 对比类验收指标当前**无法验证**，已在报告中明确标记，待用户提供数据后补测。
- 无头测试已验证：Blender 5.2 可 `--background --python` 运行自研脚本（本审计即证）。
- 系统探测工具（`wmic`）被安全策略禁用，已改用 PowerShell CIM 取数。
- 审计原始数据见 `output/audit_blender_env.json`、`output/audit_run.log`、`output/sysinfo.txt`。

---

*（本文件为持续维护文档，每次重大修改均更新；非项目结束才生成。）*

---

## 18. V0.1 交付与测试结果（2026-08-17）

### 18.1 交付内容

| 模块 | 文件 | 状态 |
|------|------|------|
| Addon 注册 | `addon/ai_figure_refiner/__init__.py` | ✅ |
| 核心 Session | `core/session.py`（RepairSession + Part + Snapshot） | ✅ |
| 日志 | `core/logging.py`（UI 集合 + stdout） | ✅ |
| 错误处理 | `core/errors.py`（AFRError + safe_run） | ✅ |
| Step Pipeline | `core/pipeline.py`（7 个步骤定义） | ✅ |
| FDM 打印参数 | `operators.py`（AFRPrintSettings） | ✅ |
| PropertyGroup | `operators.py`（AFRLogEntry / AFRPrintSettings） | ✅ |
| 算子 | `operators.py`：import / use_selected / run_diagnostics / repair_basic / rollback / next_step / prev_step | ✅ |
| N 面板 UI | `ui/panel.py`（View3D > Sidebar > AI Figure Refiner） | ✅ |
| 网格诊断 | `geometry/diagnostics.py`（12 项指标 + bbox + 体积 + 水密） | ✅ |
| 基础修复 | `geometry/repair.py`（去重 +法线 +补洞） | ✅ |
| 部署脚本 | `scripts/deploy_addon.py` | ✅ |
| 无头冒烟测试 | `scripts/test_smoke.py` | ✅ PASS |

### 18.2 无头测试结果（Blender 5.2.0 LTS background）

| 用例 | 期望 | 实际 | 结果 |
|------|------|------|------|
| Cube 诊断 | watertight=True, V=8, Q=6, vol=8.0 | V=8 E=12 F=6, Q=6, vol=8.0, watertight=True | ✅ |
| Plane 诊断（开放边界） | watertight=False, boundary>0 | V=4 E=4 F=1, boundary=4, non_manifold=4, watertight=False | ✅ |
| 重复顶点检测（人为构造） | duplicate_vertices ≥ 1 | duplicate_vertices=1 | ✅ |
| 基础修复 | 去重/法线/补洞执行 | remove_doubles -0 / recalc / holes_fill(4 edges) | ✅ |
| Rollback | 顶点计数恢复到修复前 | 4 → 4 → 4 | ✅ |
| 打印参数 | nozzle_mm 可读写 | 0.4 → 0.6 持久化 | ✅ |
| 日志 | ≥1 条 | 15 条 | ✅ |
| 7 个算子全部注册 | 全部就绪 | 全部就绪 | ✅ |

> 完整 JSON：`output/test_smoke_result.json`
> 运行日志：`output/test_smoke_run.log`

### 18.3 已知技术债（V0.1 → V0.2）

- Blender 5.2 不自动扫描 `D:/blender/5.2/scripts/addons/`，测试用 `script_path_add`（不存在，5.2 已移除）回退到 `import + register()`。用户手动安装需部署到 `%APPDATA%\Blender Foundation\Blender\5.2\scripts\addons\`，或由 `deploy_addon.py` 写入并设置 `BLENDER_USER_SCRIPTS`。
- `bpy.ops.preferences.addon_enable` 未走通（addons 路径未注册），下一步提供打包 zip 走 Edit > Preferences > Add-ons > Install 的真实路径并验证。
- BMesh 在 5.2 上索引访问需 `bm.verts.ensure_lookup_table()`（在序列上，不在 BMesh 上），已在测试中修正。
- `test_smoke_run.log` 中存在 Blender MCP / Rodin 等无关 add-on 的调试噪音，不影响本插件。

---

## 19. V0.2 交付（Phase 2.b — 可打印性分析）

### 19.1 实现内容

新增 `addon/ai_figure_refiner/geometry/printability.py`：

- **壁厚分析**：BVHTree 沿 ±face normal 双射线采样 → 局部壁厚 = min(d_in, d_out)。`mathutils.bvhtree.BVHTree.FromBMesh` 在 5.2 上直接可用；保留纯 BMesh 暴力射线回退（任意 n-gon 自动三角扇）。
- **悬垂检测**：face 中心 Z > `ground_tol`(默认 0.05) **且** `-n.z/|n| > cos(45°)`（即法线指向下半空间且超过 45° 阈）。排除贴底面（最底层无需支撑）。阈值可在算子 UI 中调。
- **悬空部件检测**：BFS 连通分量 + per-comp min-Z；min-Z > tol 判定为悬空。
- **打印验证**：水密 / 重复顶点 / 零面积 / 壁厚 vs 喷嘴&目标 / 悬垂 vs 支撑 / 悬空 / 层高 vs 喷嘴 全部归并为 `[ERROR]/[WARNING]/[INFO]` 严重度，输出 `printable / severity / issues[]`。

新增算子 `afr.run_printability` + `Scene.afr_print_json`，N 面板新增「可打印性分析」分区。

### 19.2 无头测试（`scripts/test_printability.py`，全部 PASS）

| 用例 | 断言 | 结果 |
|------|------|------|
| solid_cube (2mm) | min_wall ≈ 2.0mm, watertight, printable=True | min=2.0, watertight=true, printable=true, overhang=0 ✅ |
| thin_cube (1mm) | min_wall ≈ 1.0mm < 1.5mm 目标 → printable=False | min=1.0, 6 faces below, ERROR 触发 ✅ |
| cube + sphere（join） | 2 连通分量, 1 悬空, printable=False | 1 悬空(114 verts), 2 comps, 3 个 issue ✅ |
| 60° 倾斜 cube | 至少 1 悬垂面 | 1 overhang face, 占 16.7% 面积 ✅ |

### 19.3 Git

- `e6629a8` feat(v0.2): printability analysis — **已推送 main**

---

## 20. V0.3 交付（Phase 3 — 参考图系统）

### 20.1 实现内容

新增 `addon/ai_figure_refiner/reference/views.py`：

- **数据模型**：`Scene.afr_ref_views` CollectionProperty（4 个固定 slot FRONT/BACK/LEFT/RIGHT），每 slot 含 `image_path / camera_obj / scale / offset_x / offset_y / rotation_z`。
- **相机创建/对齐**：4 个 `AFR_RefCam_<VIEW>` 相机，按预设位置（±3.0 mm on cardinal axes）创建；`align_camera_to_bbox` 根据 bbox 自动计算镜头距离（50mm 镜头、36mm sensor、1.6× margin）并保持相机位于预设一侧。
- **图片加载**：`load_reference_image` → `bpy.data.images.load` → 挂到相机的 `background_images` collection，subtype=BACK, alpha=0.5, FIT。
- **轮廓分析**：`silhouette_edges` 解析法（每个边两邻面是否都正对相机）+ `silhouette_edge_count` / `project_outline` 便捷函数。

新增 5 个算子：`afr.ref_create_cameras / ref_align_to_bbox / ref_load_image (ImportHelper) / ref_clear_image / ref_focus_view`。N 面板新增「参考图系统」分区。

### 20.2 无头测试（`scripts/test_reference.py`，全部 PASS）

| 用例 | 断言 | 结果 |
|------|------|------|
| ensure_ref_state | 4 slot (FRONT/BACK/LEFT/RIGHT) | ✅ |
| create_cameras | 4 个 `AFR_RefCam_*` 存在 | ✅ |
| align_to_bbox | 相机从预设位置移开（>0.5） | FRONT(0,-4.22,-0.41), BACK(0,4.22,-0.41), … ✅ |
| load_images | 4 PNG（手工 zlib 写入）作为相机背景 | AFR_RefImg_<VIEW> × 4 ✅ |
| silhouette_edges | 立方体 → 6 silhouette edge | ✅ |
| project_outline | 6 个世界空间点 | ✅ |
| clear_image | 背景列表清空 | ✅ |

### 20.3 Git

- `8a9dc93` feat(v0.3): reference image system — **已推送 main**

---

## 21. V0.4 交付（Phase 4 — 部件语义识别）

### 21.1 实现内容

新增 `addon/ai_figure_refiner/semantic/parts.py`：

- **5 部件 + UNLABELED**：`PART_ID = {HAIR:1, HEAD:2, BODY:3, FABRIC:4, BASE:5}`。
- **存储**：per-vertex INT 属性 `AFR_Part`（domain=POINT）+ per-vertex BYTE_COLOR 覆盖 `AFR_PartColor`。Python 数据始终与 mesh 属性同步。
- **几何启发式**（无需 AI）：
  - BASE：bbox Z 最低 12%
  - HEAD/HAIR：bbox Z 顶部 40%；min(x_ext, y_ext) × 0.5 内的为 HEAD，外为 HAIR
  - BODY：其余
  - FABRIC refinement：法线 z<-0.3 且顶点全是 BODY 的面 → FABRIC
- **画笔**：Apply/Overwrite/Remove、Smooth（k-ring）、Flood、Grow/Shrink（迭代 boundary）、Undo（每对象 30 步栈）。
- **多视角投票**：`vote_labels(views_labels)` 占位实现，后续 AI 分割输出可直接调用。

新增 4 个算子 + N 面板「部件语义识别」分区。

### 21.2 无头测试（`scripts/test_semantic.py`，全部 PASS）

| 用例 | 断言 | 结果 |
|------|------|------|
| heuristics_label | BASE/HEAD/BODY/HAIR 均 >0 | BASE=63 BODY=20 HEAD=4 HAIR=1 ✅ |
| attribute_registered | AFR_Part (INT, POINT) | ✅ |
| color_overlay | AFR_PartColor BYTE_COLOR | ✅ |
| brush_flood_body | 全部为 BODY | ✅ |
| brush_undo | 恢复上一状态 | ✅ |
| vote_labels | 前5=HAIR, 后5=BODY | [1,1,1,1,1,3,3,3,3,3] ✅ |

### 21.3 Git

- `a0b5cbb` feat(v0.4): semantic part recognition — **已推送 main**

---

## 22. V0.5 交付（Phase 5-12 — 头发/布料/底座/合并/定向 + 3MF + AI Worker）

### 22.1 实现内容

**Phases 5-9 — Per-part 操作**（`parts_ops/hair.py` + `parts_ops/generic.py`）

- `extract_part(obj, label_id, new_name)`：复制顶点 majority-匹配 label_id 的面到新 mesh
- `solidify_part(obj, thickness)` / `solidify_fabric(obj, thickness)`：`bmesh.ops.solidify` 加厚
- `generate_hair_curves(scene, params)`：procedural 发丝（curl/noise/taper/seed）
- `curves_to_mesh(curves_obj, radius, segments)`：`bpy.ops.object.convert` 转带厚度的 mesh
- `detect_intersections(hair_obj, body_obj)` / `detect_floating(obj)`
- `generate_base(scene, source_obj, radius, height)`：bbox 下方建圆柱底座
- `merge_parts(scene, objects)`：boolean UNION modifiers
- `auto_orient(obj)`：bbox min-Z → 0

**Phase 11 — 3MF 导出器（自研）**（`exporter/three_mf.py`，纯 stdlib）

- ZIP + 3 个 XML：`[Content_Types].xml` / `_rels/.rels` / `3D/3dmodel.model`
- 用 `xml.etree.ElementTree` 写 model mesh（vertices + triangles + build item）
- 单位 `millimeter`，namespace `http://schemas.microsoft.com/3dmanufacturing/core/2015/02`
- 三角化（`bmesh.ops.triangulate` BEAUTY）+ 世界变换

**Phase 12 — AI Worker 协议**（`ai_worker/protocol.py` + `ai_worker/launcher.py`）

- 5 个支持的模型：`figure_seg / depth / normal / hair_dense / refine`
- JSON-over-stdio 协议（schema v1），`make_request` / `encode_request` / `decode_response` / `call_sync` / `mesh_to_inputs`
- `find_worker` 在 PATH + `addon/ai_figure_refiner/workers/` 查找
- `stub_worker_response` 占位响应（无 worker 时让 UI 仍可用）
- `launch_or_message` 诊断返回 `{ok, worker}` 或 `{ok:false, error, hint}`

新增 13 个算子（Hair/Fabric/Base/Merge/Orient/Export3MF/AIWorkerCheck/AIStubTest）。

### 22.2 无头测试（`scripts/test_phases_5_to_12.py`，全部 PASS）

| 用例 | 断言 | 结果 |
|------|------|------|
| hair_extract | 新对象含 hair 顶点 | verts>0 ✅ |
| hair_generate (80 strands) | curves → mesh，>100 verts | 80 strands × 3 segs ≈ ~480 ✅ |
| hair_solidify | 顶点数增加 | n0 < n1 ✅ |
| generate_base | 圆柱 mesh | ✅ |
| auto_orient | min-Z → 0 | z_min = -8.94e-08 ✅ |
| merge_parts (boolean union) | 合并后 ≥1 顶点 | ✅ |
| solidify_fabric | 顶点增加 | n0 < n1 ✅ |
| export_3mf | zip+xml，3 个文件名 | 24k verts → 50k tris，668 KB ✅ |
| 3mf_zip_structure_valid | Content_Types/_rels/3D 文件存在 + 含 `<vertices` `<triangles` `</model>` | ✅ |
| ai_protocol_roundtrip | id + model 匹配 | ✅ |
| mesh_to_inputs | vert/face 数与 mesh 一致 | 24190 / 22883 ✅ |
| ai_launcher_diagnostic | 返回 {ok, hint} | ok=false, has_hint=true ✅ |
| ai_stub_response | stub=True, ok=True | ✅ |

### 22.3 Git

- `75b583a` feat(v0.5): hair/fabric/base/merge/orient/3MF/AI worker — **已推送 main**

---

## 23. 全阶段回归（全部 PASS）

```
=== scripts/test_smoke.py              === PASS
=== scripts/test_printability.py       === PASS
=== scripts/test_reference.py          === PASS
=== scripts/test_semantic.py           === PASS
=== scripts/test_phases_5_to_12.py     === PASS
```

---

## 24. 最终交付清单（V0.5）

| 类别 | 内容 |
|------|------|
| **审计** | `报告.md` — 24 个章节（Phase 0-12 完整记录） |
| **插件代码** | `addon/ai_figure_refiner/` — 9 个子包（core, geometry, ui, reference, semantic, parts_ops, exporter, ai_worker, operators/panel/init） |
| **算子数量** | **28 个**（import/use_selected/diagnostics/repair/rollback/next/prev/printability ×1, ref ×5, semantic ×4, hair ×3, fabric/base/merge/orient/export3mf/ai ×7） |
| **N 面板分区** | 导入/源 / 步骤进度 / 网格诊断与修复 / 可打印性 / 参考图系统 / 部件语义识别 / 头发精修 / 布料·底座·合并·定向 / 导出 / AI Worker / FDM 打印参数 / 日志 |
| **诊断能力** | V/E/F、Tri/Q/NG、非流形、边界、重复顶点、零面积、坏法线、连通分量、bbox、volume、水密 |
| **修复能力** | 去重 + 法线重算 + 补洞 + 快照回滚 + 加厚 (Solidify) + Boolean Union |
| **可打印性** | 壁厚 (BVH 射线) / 悬垂 / 悬空 / 验证 (ERROR/WARNING/INFO) |
| **参考图** | 4 视图 + 相机 + bbox 对齐 + 背景图 + 轮廓分析 |
| **部件语义** | 5 类 + 几何启发式 + 6 种画笔 + 投票 |
| **头发** | 提取 + 加厚 + 程序化生成(curl/noise/taper) + 转换 |
| **底座/合并/定向** | 圆柱底座 + Boolean Union + 自动落地 |
| **3MF 导出** | 自研 ZIP+XML，668 KB，结构合法 |
| **AI Worker** | 5 模型协议 + worker 查找 + Stub 响应 |
| **测试** | 5 个无头脚本（smoke/printability/reference/semantic/phases_5_12），全部 PASS |
| **文档** | 报告.md + README.md + CHANGELOG.md（V0.1-V0.5） |
| **GitHub** | 7 commits，`Klisuaiji/AI-Figure-Model-Refiner` main 分支已同步 |

---

## 25. 后续路线（V0.6+ — 交付后可继续完善）

- **真实 AI Worker 实现**：在 `addon/ai_figure_refiner/workers/` 落地 Python venv + ONNX Runtime + SAM/Dino 推理脚本，端到端跑通"参考图 → 部件分割"。
- **3MF 多对象/嵌套 components**：当前 V0.5 仅单 object；V0.6 加 build items + components 嵌套，支持多部件分别 3MF。
- **Slice 集成**：直接调用 PrusaSlicer / OrcaSlicer CLI 验证 G-code 路径。
- **打包 zip 提供给 Blender 偏好安装**：走 `Edit > Preferences > Add-ons > Install` 真路径而非 import 注册。
- **Voronoi 微结构生成**（手办内部填充减重 + 强度）。
- **AI 训练数据收集**：上传匿名 before/after/Reference 三元组到云端，迭代模型。
